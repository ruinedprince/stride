# Node
node_modules/
dist/

# GraphHopper — big binaries and generated graph, not versioned
backend/graphhopper-web-*.jar
backend/data/*.osm
backend/data/*.osm.pbf
backend/data/*.osm.gz
backend/data/*.geojsonl
backend/graph-cache/

# OS / editor
.DS_Store
Thumbs.db

# Shade custom models are generated per-hour but consumed per-request from the
# display geojson (frontend builds the model in JS), so they don't need to be
# versioned. green.json IS referenced by the foot_green profile — keep it.
backend/custom_models/shade_*.json

# Python
__pycache__/

# GraphHopper elevation tiles (downloaded)
backend/elevation-cache/
